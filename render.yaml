services:
  - type: worker
    name: tiktok-lyrics-bot
    runtime: python
    buildCommand: |
      apt-get update && apt-get install -y ffmpeg nodejs npm
      pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
